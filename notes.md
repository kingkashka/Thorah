#intro to Express

    #Initialize a new node project
        - npm init -y
        - npm install 

    #vocabulary

        #Route
            * An event listener for http request

        #Endpoint
            *"/item"  "/user"

        #Port
            * localhost:9000/

    #Nodemon
        * npm install -g nodemon